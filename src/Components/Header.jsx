import React from 'react';

export default function Header({ props }) {
  return (
    <div style={styles.container}>
      <div>header</div>
    </div>
  );
}

const styles = {
  container: {
    height: 100,
  },
};
