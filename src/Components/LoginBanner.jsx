import React from 'react';

export default function LoginBanner({ props }) {
  return (
    <div style={styles.container}>
      <div>login</div>
    </div>
  );
}

const styles = {
  container: {
    width: '40vh',
  },
};
