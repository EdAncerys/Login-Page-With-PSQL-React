import './App.css';

import React from 'react';

export default function App({ props }) {
  return (
    <div className="App" style={styles.container}>
      <div>App</div>
    </div>
  );
}

const styles = {
  container: {},
};
